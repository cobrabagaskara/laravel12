{
  "name": "Laravel 12 & Filament 4 Codespace",
  "image": "mcr.microsoft.com",
  "forwardPorts": [80, 8000, 5173],
  "portsAttributes": {
    "8000": {
      "label": "Laravel App",
      "onAutoForward": "openBrowser"
    },
    "5173": {
      "label": "Vite",
      "onAutoForward": "silent"
    }
  },
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/home/vscode/.config/composer/vendor/bin"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "mikestead.laravel-extension-pack",
        "austenc.tailwind-fold",
        "Vue.volar",
        "Vue.vscode-typescript-vue-plugin"
      ]
    }
  },
  "postCreateCommand": "composer global require hirak/prestissimo --no-interaction && cp .env.example .env && composer install && npm install && php artisan key:generate && php artisan migrate --force && php artisan filament:install --panels && npm run dev"
}
